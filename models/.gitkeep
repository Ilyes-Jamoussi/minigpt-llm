# Trained artifacts (config.json, tokenizer, metrics.json) are written here.
# Large weights (*.safetensors) are gitignored; publish them via Git LFS or a
# model hub when deploying. This file just keeps the directory in git.
